name: Build, Push & Deploy

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - service: backend-stage-0
            context: ./backend/stage-0
          - service: backend-stage-1
            context: ./backend/stage-1
          - service: backend-stage-2
            context: ./backend/stage-2

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.service }}
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          push: true
          tags: ghcr.io/tony-nyagah/hng14/backend/${{ matrix.service }}:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Hetzner
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HETZNER_HOST }}
          username: ${{ secrets.HETZNER_USER }}
          key: ${{ secrets.HETZNER_SSH_KEY }}
          port: 22
          key_path: ""
          debug: true
          script: |
            cd /root/hng14
            git pull origin main
            mkdir -p /root/traefik
            if [ -f /root/traefik/traefik.yml ]; then rm /root/traefik/traefik.yml; fi
            cp /root/hng14/traefik.yml /root/traefik/traefik.yml
            touch /root/traefik/acme.json
            chmod 600 /root/traefik/acme.json
            docker compose -f docker-compose.prod.yml pull
            docker compose -f docker-compose.prod.yml up -d --force-recreate
